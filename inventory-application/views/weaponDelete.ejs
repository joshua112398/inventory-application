<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head'); %>
    <title><%= title %></title>
  </head>
  <body>
    <div class="d-flex vh-100">
      <%- include('./partials/nav'); %>
      <main class="container text-center p-4 d-flex flex-column align-items-center">
        <%# Show a list of characters that have the weapon equipped as a warning, but user does not need %>
        <%# to delete them before deleting the weapon, the characters will just have no weapon equipped %>
        <% if (characters.length) { %>
          <p> These characters currently have this weapon equipped. They will end up with no weapon if you proceed.</p>
          <% characters.forEach((character) => { %>
            <a href="<%= character.url %>"><%= character.name %></a>
          <% }) %>
        <% } %>
        <form method="POST" action="" class="d-flex flex-column form-width">
          <p>Are you sure you want to delete <%= weapon.name %>?</p>
          <button type="submit" class="btn btn-primary">Delete</button>
        </form>
      </main>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
  </body>
</html>